<p>ここが<%= @room.room_name %>です </p>
PL<%= @pl.count %>人</br>GM<%= @gm.count %>人

<% if flash[:notice] %>
  <div class="alert alert-success">
    <%= flash[:notice] %>
  </div>
<% end %>
<% if flash[:alert] %>
  <div class="alert alert-danger">
   <%= flash[:alert] %>
  </div>
<% end %>

<div id="chats_log">
	　<%= render 'chats/chat', room: @room %>
</div>

     <%= form_for([@room, @role]) do |f| %>
     	人狼<%= f.number_field :jinrou%>
     	村人<%= f.number_field :murabito  %>
     	占い師<%= f.number_field :uranai %>
     	霊能者<%= f.number_field :reino %>
     	狂人<%= f.number_field :kyojin %>
        <%= f.hidden_field :room_id, :value => @room.id
 %>
        <%= f.submit "配役決定" %>
     <% end %>
<%= link_to "ゲームスタート!",room_rolecards_path(@room.id), method: :post %>

 <p><%= current_user.name %><%= current_user.job %> 画像</p>
 	<%= form_tag(room_chats_path(@room.id), :remote => true, :id =>"chats_form" ) do %>
 	<input type="text" name="content"> 
 	<%= submit_tag "発言", :id =>"sub_btn" %>
 	<% end %>

<%#= link_to "役をつける",room_rolecards_path(@room.id), method: :post %>
<%= link_to "ゲームに参加する",room_jobs_path(@room.id), method: :post %>
<%= link_to "退出する",room_joins_path(@join.room_id,@room.id), method: :delete %>
<%= link_to "観戦する",room_jobs_path(@room.id), method: :delete %>
